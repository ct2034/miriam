sudo: required
dist: trusty
language: generic

env:
  global:
    - ROS_DISTRO="melodic"
install:
  - git clone --depth=1 -b master https://github.com/ros-industrial/industrial_ci.git .industrial_ci
script:
  - .industrial_ci/travis.sh